export function AdminSidebar() {
  return (
    <nav className="flex flex-col gap-2 p-4">
      <span className="text-sm font-semibold">Administración</span>
      {/* TODO: NavLinks for admin sections */}
    </nav>
  )
}
